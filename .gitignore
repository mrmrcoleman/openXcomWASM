# Emscripten / WASM build
build-wasm/
*.wasm
*.js
*.worker.js
*.data
*.mem
# Allow web assets (service worker, bundled libs)
!web/*.js
# dist/ is built by CI — don't commit it
dist/

# OpenXcom build dirs (when fork lives inside this repo)
OpenXcom/build/
OpenXcom/build-*/
OpenXcom/obj/

# IDE and OS
.idea/
.vscode/
.DS_Store
*.swp
*~

# Local env
.env
.env.local

